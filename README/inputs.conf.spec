[mongo_admin://<name>]
* Streams MongoDB administrative events

server = <value>
port = <value>
commands = <value>

[mongo_db://<name>]
* Streams MongoDB database statistics

server = <value>
port = <value>
database = <value>
commands = <value>

[mongo_collstats://<name>]
* Streams MongoDB collection statistics

server = <value>
port = <value>
database = <value>
collections = <value>

[mongo_logs://<name>]
* Streams MongoDB log events

server = <value>
port = <value>
